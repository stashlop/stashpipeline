pipeline {
    agent any 
    stages {       
        stage('Compile') {
            steps {
                echo 'Compile the source code' 
                echo 'my name is stash tony lopes'
            }
        }
        stage ('abc'){
         echo 'build the source code' 
        }
        }
      }
}
